{
  "name": "osint-globe",
  "private": true,
  "version": "0.1.0",
  "description": "An open-source OSINT world: live flights, satellites, vessels, cameras and 3D geometry on a single zoomable globe.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @osint-globe/gateway & npm run dev --workspace @osint-globe/web",
    "dev:gateway": "npm run dev --workspace @osint-globe/gateway",
    "dev:web": "npm run dev --workspace @osint-globe/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
